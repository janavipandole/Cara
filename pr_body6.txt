Closes #1425

## Changes
- login.js: Added canvas-based geometric CAPTCHA challenge generator:
  - Renders 5 random alphanumeric characters with rotated/styled geometric shapes behind each
  - Decorative circles, rects, triangles as background noise
  - Noise lines and dots to prevent OCR
- login.js: Tracks login attempts — CAPTCHA activates after first failed login
- login.js: Verifies CAPTCHA code on all subsequent submissions
- login.html: Added hidden CAPTCHA section with canvas, input, and refresh button
- login.css: Added CAPTCHA section, canvas, and refresh button styles (dark-mode aware)
