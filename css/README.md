# CSS layering

Design tokens and stylesheet roles for contributors:

- `tokens.css` — design tokens only
- `style.css` — canonical components / layout
- `global.css` — deprecated shim (imports `style.css`)
- `bundle.css` — deprecated shim (imports `style.css`)
